# Connect the WhiteBooks sandbox

On the computer that runs Karobar, open the Karobar folder and run:

```sh
npm run gsp:credentials
```

Type each value when asked. The client secrets and password stay hidden while you type. Do not send the values to anyone. When every value is saved, close the terminal.
